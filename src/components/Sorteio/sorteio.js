import React from 'react';
import './sorteio.css'

const sorteio = (props) => {
  return (
    <div id="sorteio" className="wrapperItem">
      {props.display}
    </div>
  )
}

export default sorteio;